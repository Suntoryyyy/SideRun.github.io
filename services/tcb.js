import cloudbase from '@cloudbase/js-sdk';

// We will initialize your Tencent CloudBase app here once you give me the Environment ID
/*
export const app = cloudbase.init({
  env: 'YOUR-ENV-ID' // e.g., 'siderun-123456'
});

export const auth = app.auth();
export const db = app.database();
*/

// Placeholder until you grab the keys
export default cloudbase;
